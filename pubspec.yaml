name: kelasfun
description: "Aplikasi manajemen kelas offline-first"
publish_to: 'none'
version: 1.4.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
