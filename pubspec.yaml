name: kelasfun
description: "Aplikasi manajemen kelas offline-first"
publish_to: 'none'
version: 1.4.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  drift: ^2.28.0
  provider: ^6.1.0
  intl: ^0.19.0
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  drift_dev: ">=2.22.0 <2.28.1"
  build_runner: ^2.4.0
